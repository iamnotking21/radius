# 30 · DECISION LOG (APPEND ONLY — never edit or delete a row)
<!-- big/irreversible → also write full ADR in docs/adr/ -->

| # | date | decision | why | adr |
|---|---|---|---|---|
| 1 | 2026-08-04 | native Swift + Kotlin, no cross-platform | BLE mesh + bg radio unbuildable well on RN/Flutter. cost 1.8x, worth it. | ADR-001 |
| 2 | 2026-08-04 | Go backend, Connect-RPC, monorepo | concurrency/eng ratio; 1 proto → gRPC+JSON; atomic cross-consumer changes | ADR-002 |
| 3 | 2026-08-04 | self-host OSS on Tencent CVM Singapore | zero licence cost, no lock-in, no ICP filing, ~30-50ms to Manila | ADR-003 |
| 4 | 2026-08-04 | BLE: rotating ephemeral IDs, banded distance, single-hop P0 | EN-protocol proven privacy model; mesh relay = P4 not MVP | ADR-004 |
| 5 | 2026-08-04 | vodozemac not libsignal | libsignal AGPL, we ship closed source | ADR-004 |
| 6 | 2026-08-04 | Valkey not Redis; SeaweedFS not MinIO; OpenTofu not Terraform | licence changes = commercial risk | ADR-003 |
| 7 | 2026-08-04 | rules-based matching first, ML at P3 | no training data on day1; explainable beats bad model | — |
| 8 | 2026-08-04 | Radar framed as FOREGROUND destination | iOS bg BLE restricted by Apple; honest UX > broken promise | ADR-004 |
| 9 | 2026-08-04 | calling = 1:1 P2P WebRTC, no SFU, coturn only | media never touches infra ⇒ near-zero cost + genuinely E2E. SFU for 1:1 adds cost+latency for nothing | ADR-005 |
| 10 | 2026-08-04 | calls invited, never cold-rung | strangers cold-ringing strangers is why dating-app calling dies | ADR-005 |
| 11 | 2026-08-04 | calls never recorded; no content column in schema | wiretap law in 2-party-consent jurisdictions + trust. schema-level guarantee > policy | ADR-005 |
| 12 | 2026-08-04 | conversion via legitimate psych only, zero dark patterns | FTC ROSCA enforcement live; EU DFA draft Q3/Q4 2026; refund+chargeback tail risk; and we SELL honesty | ADR-006 |
| 13 | 2026-08-04 | no-card GIFT (E7) instead of card-required trial | near-total uptake, converts on demonstrated value not forgetfulness — which is the mechanism regulators target | ADR-006 |
| 14 | 2026-08-04 | BLE voice = push-to-talk clips only, P4 | full duplex not achievable at ~100-300kbps. do not fake it | ADR-005 |
| 15 | 2026-08-04 | mobile = Kotlin Multiplatform shared core. REVERSES row 1 in part | founder direction. zero app code written = cheapest possible moment. kills protocol-divergence class of bug: ONE codec/key-schedule/ratchet impl, not two | ADR-007 |
| 16 | 2026-08-04 | UI stays fully native. NO Compose Multiplatform on iOS | premium product; radar = continuously-animating canvas; CMP-iOS scroll/text/a11y still rough. ADR-001 native-UI argument survives intact | ADR-007 |
| 17 | 2026-08-04 | SQLDelight replaces GRDB + Room | shared module cannot own 2 platform DBs. SQLDelight = compile-checked raw SQL = mobile twin of sqlc. Room-KMP rejected as ORM | ADR-007 |
| 18 | 2026-08-04 | KMP does NOT reduce BLE effort. radio still written twice via expect/actual | stated up front so nobody plans the Phase 0 spike as if it shrank. it did not | ADR-007 |
| 19 | 2026-08-04 | Mac becomes MANDATORY infra (dev + macOS CI runner) | Kotlin/Native iOS targets compile on macOS only. no Windows/Linux path exists | ADR-007 |
| 20 | 2026-08-04 | mobile/shared public API = contract under contract-first law | 2 consumers (ios-swift, android-kotlin). unilateral change = the exact failure mode ORCHESTRATION §3 exists to stop | ADR-007 |
| 21 | 2026-08-04 | iOS BleRadio seam = SWIFT PORT injected into Kotlin. NOT Kotlin/Native calling CoreBluetooth from iosMain. resolves ios-swift assumption 14 vs android-kotlin impl | radio is the moat; write it where the tooling is best. Swift gets Instruments Energy Log, real debugging, and native state-restoration semantics. Kotlin/Native objc-interop delegates would be the hardest-to-debug code in the product, in the one place we cannot afford mystery. iOS state restoration ownership goes to ios-swift. shared declares the interface, Swift conforms. | amend ADR-007 if it hardens |
| 22 | 2026-08-04 | KSP not kapt for Hilt on Kotlin2 | kapt legacy + slow on Kotlin 2.x. build-plugin only, no runtime dep, Hilt already approved ⇒ not a §8 escalation | — |
| 23 | 2026-08-04 | BLE = TWO CARRIERS, one 19B frame. A=service data (Android). B=GATT read (iOS). | iOS cannot emit service/mfr data in ANY state (CBPeripheralManager honours only LocalName+ServiceUUIDs). confirmed independently by ios-swift AND android-kotlin. frame does not degrade, it changes carrier ⇒ ver/txpower/flags keep their home, invariant 4 untouched. cost: connection per peer per epoch + carrier B makes the SCANNER transmit | BLE spec v0 |
| 24 | 2026-08-04 | "eid as 128-bit service UUID" REJECTED as primary carrier | fits 16B exactly but evicts ver/txpower/flags. losing tx_power_cal alone = ±15dB against 13-15dB bands = up to a FULL BAND wrong. kept as F3, iOS-fg passive optimisation only | BLE spec v0 §5.0-b |
| 25 | 2026-08-04 | stable identifier to make iOS background work = PRE-REJECTED IN WRITING (F7) | it is a NO-GO, not a trade. written down now so the argument cannot be re-run under deadline pressure in month nine | BLE spec v0 |
| 26 | 2026-08-04 | NO BONDING, NO PAIRING, EVER. all GATT chars declared with no security permissions | a bond exchanges an IRK = permanent RPA-resolution key = voids invariant 5 completely. now a PR checklist item | BLE spec v0 |
| 27 | 2026-08-04 | eid rotation synchronised to the GLOBAL UTC boundary. NO jitter. | counter-intuitive but load-bearing: staggering rotation shrinks the anonymity set from the whole population to ONE and makes every rotation linkable | BLE spec v0 |
| 28 | 2026-08-04 | RSSI (raw/filtered/adjusted) must not cross the shared boundary, reach the server, or appear in logs/analytics/debug builds | RSSI IS LOCATION DATA — 3 receivers ⇒ multilateration. server gets (account, band, epoch), nothing finer | BLE spec v0 |
| 29 | 2026-08-04 | displayed metres = f(session_salt, peer_id, band) ONLY. never a function of RSSI. | jitter added to a real value is defeated by averaging. ours contains no real value to recover | BLE spec v0 |
| 30 | 2026-08-04 | invariant 5 is a PER-DEVICE-MODEL property, not per-platform | RPA rotation uncontrollable on both OSes, controller-firmware dependent on Android. pre-decided: MAC slower ⇒ lengthen eid period; MAC never rotates ⇒ exclude model or ship scan-only. NEVER rotate payload against a fixed MAC and call it rotation — that is worse than not rotating, it merely looks like privacy | BLE spec v0 KEY_SCHEDULE §4.3 |
| 32 | 2026-08-04 | account_key SERVER-ISSUED | founder call. buys multi-device + recovery + revocation + abuse investigation. COSTS: server can derive every eid a user will ever broadcast, retroactively. we can no longer truthfully say "we cannot identify who was broadcasting". breach = retroactive location-history breach, not point-in-time. 7 mandatory mitigations M1-M7. invariants 4+5 UNCHANGED — third-party air observer still learns nothing; it is the OPERATOR that entered the threat model | ADR-008 |
| 33 | 2026-08-04 | ANDROID-FIRST. iOS deferred, not cancelled. | founder call. no Mac (B4) ⇒ iOS was unbuildable anyway; deferring converts a hard blocker into a scheduled one. consequence: B8 (RPA co-rotation on Android) is now THE top technical risk — Android is the entire product surface, so an OEM that cannot rotate its MAC is not a gap, it is a hole in the whole product | — |
| 34 | 2026-08-04 | keep 0xFDA9 provisional in dev; CI gate blocks RELEASE builds containing it. SIG application gated behind legal-entity formation. | 128-bit UUID cannot work for Carrier A: adv budget is 30/31 bytes WITH a 16-bit UUID, and 128-bit costs 16 more. so a SIG 16-bit allocation is mandatory, not preferred. but SIG Adopter membership needs a legal entity, which is itself an unresolved open question ⇒ sequence is entity → Adopter → UUID, and the CI gate is what stops the provisional value shipping while we wait | ADR-004, B9 |
| 35 | 2026-08-04 | EXACTLY ONE advertising device per account. all others SCAN_ONLY, fail closed. | ADR-008 consequence nobody priced: two devices sharing account_key emit a BYTE-IDENTICAL eid from two locations. a stationary 2nd device becomes a live-eid oracle at a KNOWN address = stalking primitive needing no tailing, and it permanently bridges both MACs. invariant 5 defeated by a second route. multi-device is real for account ACCESS, false for Radar BROADCAST | ADR-008 §9 |
| 36 | 2026-08-04 | P1 · never ingest observed ephemeral_ids on ANY endpoint. never operate/fund/contract/ingest from a BLE scanning network. | derivation power (ADR-008) is only location data if we ALSO hold captures. "let clients upload the eids they saw so we can debug discovery" is how this becomes mass surveillance while sounding reasonable. pre-rejected in writing like F7 so it cannot be re-argued under deadline | ADR-008 §10.3 |
| 37 | 2026-08-04 | RPA pass criterion = BIJECTION between AdvA and eid over a capture. NOT "the address changes". 3× nRF52840, one pinned per channel 37/38/39. | one dongle cannot distinguish a MISSED packet from an ABSENT one, and we are asserting absence — cheapest possible way to record a FALSE PASS on the highest-severity question in the protocol. also traps PHASE-OFFSET: BLE default RPA timeout is 900s = our epoch exactly, so same-period-wrong-phase is a PERMANENT bridge that a period-only test reports as a pass | KEY_SCHEDULE §4.3 |
| 31 | 2026-08-04 | shared API v0 gated. entry = create(config,radio,scope) · setVisibility(enum) not setGhostMode(Bool) · ByteArray not hex String · shutdown() AND weak both required | ByteArray reasoning generalises decision 28: a String eid is log-shaped and survives into crash reports + analytics. make privacy-critical values awkward to print | 40-contracts |
