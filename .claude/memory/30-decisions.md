# 30 · DECISION LOG (APPEND ONLY — never edit or delete a row)
<!-- big/irreversible → also write full ADR in docs/adr/ -->

| # | date | decision | why | adr |
|---|---|---|---|---|
| 1 | 2026-08-04 | native Swift + Kotlin, no cross-platform | BLE mesh + bg radio unbuildable well on RN/Flutter. cost 1.8x, worth it. | ADR-001 |
| 2 | 2026-08-04 | Go backend, Connect-RPC, monorepo | concurrency/eng ratio; 1 proto → gRPC+JSON; atomic cross-consumer changes | ADR-002 |
| 3 | 2026-08-04 | self-host OSS on Tencent CVM Singapore | zero licence cost, no lock-in, no ICP filing, ~30-50ms to Manila | ADR-003 |
| 4 | 2026-08-04 | BLE: rotating ephemeral IDs, banded distance, single-hop P0 | EN-protocol proven privacy model; mesh relay = P4 not MVP | ADR-004 |
| 5 | 2026-08-04 | vodozemac not libsignal | libsignal AGPL, we ship closed source | ADR-004 |
| 6 | 2026-08-04 | Valkey not Redis; SeaweedFS not MinIO; OpenTofu not Terraform | licence changes = commercial risk | ADR-003 |
| 7 | 2026-08-04 | rules-based matching first, ML at P3 | no training data on day1; explainable beats bad model | — |
| 8 | 2026-08-04 | Radar framed as FOREGROUND destination | iOS bg BLE restricted by Apple; honest UX > broken promise | ADR-004 |
| 9 | 2026-08-04 | calling = 1:1 P2P WebRTC, no SFU, coturn only | media never touches infra ⇒ near-zero cost + genuinely E2E. SFU for 1:1 adds cost+latency for nothing | ADR-005 |
| 10 | 2026-08-04 | calls invited, never cold-rung | strangers cold-ringing strangers is why dating-app calling dies | ADR-005 |
| 11 | 2026-08-04 | calls never recorded; no content column in schema | wiretap law in 2-party-consent jurisdictions + trust. schema-level guarantee > policy | ADR-005 |
| 12 | 2026-08-04 | conversion via legitimate psych only, zero dark patterns | FTC ROSCA enforcement live; EU DFA draft Q3/Q4 2026; refund+chargeback tail risk; and we SELL honesty | ADR-006 |
| 13 | 2026-08-04 | no-card GIFT (E7) instead of card-required trial | near-total uptake, converts on demonstrated value not forgetfulness — which is the mechanism regulators target | ADR-006 |
| 14 | 2026-08-04 | BLE voice = push-to-talk clips only, P4 | full duplex not achievable at ~100-300kbps. do not fake it | ADR-005 |
| 15 | 2026-08-04 | mobile = Kotlin Multiplatform shared core. REVERSES row 1 in part | founder direction. zero app code written = cheapest possible moment. kills protocol-divergence class of bug: ONE codec/key-schedule/ratchet impl, not two | ADR-007 |
| 16 | 2026-08-04 | UI stays fully native. NO Compose Multiplatform on iOS | premium product; radar = continuously-animating canvas; CMP-iOS scroll/text/a11y still rough. ADR-001 native-UI argument survives intact | ADR-007 |
| 17 | 2026-08-04 | SQLDelight replaces GRDB + Room | shared module cannot own 2 platform DBs. SQLDelight = compile-checked raw SQL = mobile twin of sqlc. Room-KMP rejected as ORM | ADR-007 |
| 18 | 2026-08-04 | KMP does NOT reduce BLE effort. radio still written twice via expect/actual | stated up front so nobody plans the Phase 0 spike as if it shrank. it did not | ADR-007 |
| 19 | 2026-08-04 | Mac becomes MANDATORY infra (dev + macOS CI runner) | Kotlin/Native iOS targets compile on macOS only. no Windows/Linux path exists | ADR-007 |
| 20 | 2026-08-04 | mobile/shared public API = contract under contract-first law | 2 consumers (ios-swift, android-kotlin). unilateral change = the exact failure mode ORCHESTRATION §3 exists to stop | ADR-007 |
| 21 | 2026-08-04 | iOS BleRadio seam = SWIFT PORT injected into Kotlin. NOT Kotlin/Native calling CoreBluetooth from iosMain. resolves ios-swift assumption 14 vs android-kotlin impl | radio is the moat; write it where the tooling is best. Swift gets Instruments Energy Log, real debugging, and native state-restoration semantics. Kotlin/Native objc-interop delegates would be the hardest-to-debug code in the product, in the one place we cannot afford mystery. iOS state restoration ownership goes to ios-swift. shared declares the interface, Swift conforms. | amend ADR-007 if it hardens |
| 22 | 2026-08-04 | KSP not kapt for Hilt on Kotlin2 | kapt legacy + slow on Kotlin 2.x. build-plugin only, no runtime dep, Hilt already approved ⇒ not a §8 escalation | — |
| 23 | 2026-08-04 | BLE = TWO CARRIERS, one 19B frame. A=service data (Android). B=GATT read (iOS). | iOS cannot emit service/mfr data in ANY state (CBPeripheralManager honours only LocalName+ServiceUUIDs). confirmed independently by ios-swift AND android-kotlin. frame does not degrade, it changes carrier ⇒ ver/txpower/flags keep their home, invariant 4 untouched. cost: connection per peer per epoch + carrier B makes the SCANNER transmit | BLE spec v0 |
| 24 | 2026-08-04 | "eid as 128-bit service UUID" REJECTED as primary carrier | fits 16B exactly but evicts ver/txpower/flags. losing tx_power_cal alone = ±15dB against 13-15dB bands = up to a FULL BAND wrong. kept as F3, iOS-fg passive optimisation only | BLE spec v0 §5.0-b |
| 25 | 2026-08-04 | stable identifier to make iOS background work = PRE-REJECTED IN WRITING (F7) | it is a NO-GO, not a trade. written down now so the argument cannot be re-run under deadline pressure in month nine | BLE spec v0 |
| 26 | 2026-08-04 | NO BONDING, NO PAIRING, EVER. all GATT chars declared with no security permissions | a bond exchanges an IRK = permanent RPA-resolution key = voids invariant 5 completely. now a PR checklist item | BLE spec v0 |
| 27 | 2026-08-04 | eid rotation synchronised to the GLOBAL UTC boundary. NO jitter. | counter-intuitive but load-bearing: staggering rotation shrinks the anonymity set from the whole population to ONE and makes every rotation linkable | BLE spec v0 |
| 28 | 2026-08-04 | RSSI (raw/filtered/adjusted) must not cross the shared boundary, reach the server, or appear in logs/analytics/debug builds | RSSI IS LOCATION DATA — 3 receivers ⇒ multilateration. server gets (account, band, epoch), nothing finer | BLE spec v0 |
| 29 | 2026-08-04 | displayed metres = f(session_salt, peer_id, band) ONLY. never a function of RSSI. | jitter added to a real value is defeated by averaging. ours contains no real value to recover | BLE spec v0 |
| 30 | 2026-08-04 | invariant 5 is a PER-DEVICE-MODEL property, not per-platform | RPA rotation uncontrollable on both OSes, controller-firmware dependent on Android. pre-decided: MAC slower ⇒ lengthen eid period; MAC never rotates ⇒ exclude model or ship scan-only. NEVER rotate payload against a fixed MAC and call it rotation — that is worse than not rotating, it merely looks like privacy | BLE spec v0 KEY_SCHEDULE §4.3 |
| 32 | 2026-08-04 | account_key SERVER-ISSUED | founder call. buys multi-device + recovery + revocation + abuse investigation. COSTS: server can derive every eid a user will ever broadcast, retroactively. we can no longer truthfully say "we cannot identify who was broadcasting". breach = retroactive location-history breach, not point-in-time. 7 mandatory mitigations M1-M7. invariants 4+5 UNCHANGED — third-party air observer still learns nothing; it is the OPERATOR that entered the threat model | ADR-008 |
| 33 | 2026-08-04 | ANDROID-FIRST. iOS deferred, not cancelled. | founder call. no Mac (B4) ⇒ iOS was unbuildable anyway; deferring converts a hard blocker into a scheduled one. consequence: B8 (RPA co-rotation on Android) is now THE top technical risk — Android is the entire product surface, so an OEM that cannot rotate its MAC is not a gap, it is a hole in the whole product | — |
| 34 | 2026-08-04 | keep 0xFDA9 provisional in dev; CI gate blocks RELEASE builds containing it. SIG application gated behind legal-entity formation. | 128-bit UUID cannot work for Carrier A: adv budget is 30/31 bytes WITH a 16-bit UUID, and 128-bit costs 16 more. so a SIG 16-bit allocation is mandatory, not preferred. but SIG Adopter membership needs a legal entity, which is itself an unresolved open question ⇒ sequence is entity → Adopter → UUID, and the CI gate is what stops the provisional value shipping while we wait | ADR-004, B9 |
| 35 | 2026-08-04 | EXACTLY ONE advertising device per account. all others SCAN_ONLY, fail closed. | ADR-008 consequence nobody priced: two devices sharing account_key emit a BYTE-IDENTICAL eid from two locations. a stationary 2nd device becomes a live-eid oracle at a KNOWN address = stalking primitive needing no tailing, and it permanently bridges both MACs. invariant 5 defeated by a second route. multi-device is real for account ACCESS, false for Radar BROADCAST | ADR-008 §9 |
| 36 | 2026-08-04 | P1 · never ingest observed ephemeral_ids on ANY endpoint. never operate/fund/contract/ingest from a BLE scanning network. | derivation power (ADR-008) is only location data if we ALSO hold captures. "let clients upload the eids they saw so we can debug discovery" is how this becomes mass surveillance while sounding reasonable. pre-rejected in writing like F7 so it cannot be re-argued under deadline | ADR-008 §10.3 |
| 37 | 2026-08-04 | RPA pass criterion = BIJECTION between AdvA and eid over a capture. NOT "the address changes". 3× nRF52840, one pinned per channel 37/38/39. | one dongle cannot distinguish a MISSED packet from an ABSENT one, and we are asserting absence — cheapest possible way to record a FALSE PASS on the highest-severity question in the protocol. also traps PHASE-OFFSET: BLE default RPA timeout is 900s = our epoch exactly, so same-period-wrong-phase is a PERMANENT bridge that a period-only test reports as a pass | KEY_SCHEDULE §4.3 |
| 38 | 2026-08-04 | 0xFDA9 release gate: SOURCE SCAN is authoritative. APK artifact scan is defence-in-depth ONLY. | qa-test disassembled a real release APK with dexdump before writing the claim down, and found R8 DECOMPOSES the 16-bit constant into two byte loads that never appear as one grep-able token. ordinary optimiser behaviour, not adversarial. an artifact-only gate would have passed a shipping violation | devops/ci/gates/release_uuid_gate.sh |
| 39 | 2026-08-04 | invariant-1 gate strips comments before scanning and does NOT pattern-match bare `heading` | calibrated against real code: it flagged androidx.compose.ui.semantics.heading() (a legitimate a11y API — banning it would have degraded accessibility to satisfy a privacy grep) and the English idiom "load-bearing" in our own prose. a gate with false positives gets disabled by the first engineer it blocks, which is worse than no gate | devops/ci/gates/no_map_no_bearing_gate.sh |
| 40 | 2026-08-04 | crypto moves to expect/actual over PLATFORM crypto (javax.crypto.Mac / CryptoKit). hand-written impl RETAINED, moved to commonTest as a DIFFERENTIAL ORACLE run against the platform backend every CI build. | security-privacy found the stated choice was a false dichotomy: it was "hand-roll vs add a 3rd-party dep", but expect/actual over platform APIs is neither — no new dependency, no §8 escalation, no supply-chain surface. NOT a correctness fix: exhaustive differential fuzz vs the JDK found ZERO mismatches across every boundary class. this is about SOURCING, and it ends with MORE assurance than either option alone — shipped code is platform-validated AND a second independent impl cross-checks it. ~1 day. | ADR-007 note |
| 41 | 2026-08-04 | `THE LINE` in Crypto.kt becomes a CI GATE, not a comment. build fails on any new import or new function in that file. | the real risk was never "is this right today" — it is month nine, when someone needs AES for the encrypted DB and Crypto.kt is sitting there looking like the obvious place. a comment does not survive deadline pressure; a red build does | devops/ci |
| 42 | 2026-08-04 | KEEP conformanceState(). rename its guard test to noNewRssiShapedAccessorAppears and add a real CI grep for `$shared_debug` outside test sources. | deleting it would make banding.json's adjusted_dbm assertions unexecutable — the exact assertions that caught the outlier-gate livelock and the warm-up ratchet. trading two real bugs' worth of regression cover for a paper property is a bad trade. but the reflection test only sees PUBLIC members, so it never checked what its comment claimed; it is a lint, not a proof, and must be named as one | security review |
| 43 | 2026-08-04 | Kotlin `internal` is NOT JVM access control. every comment claiming otherwise is wrong and must be corrected. | proven, not argued: from a foreign compilation unit with NO reflection, security-privacy read the raw 32-byte account_key out of a live KeyRingEntry, MUTATED it (the accessor returned the live array, not a copy), and derived an arbitrary epoch's eid — the precise thing the file comments say the API shape prevents. any 3rd-party SDK in the Android process (push, IAP, crash reporting — all approved) can do the same | security review |
| 44 | 2026-08-04 | scan FOREGROUND = SCAN_MODE_BALANCED, NOT LOW_LATENCY | AOSP window/interval for LOW_LATENCY is 4096/4096 = 100% DUTY — 4x our contracted 30%. there is no path from a continuous receiver to <4%/hr. BALANCED is 1024/4096 = 25%, nearest Android offers. the scaffold shipped the battery contract already broken and a build would never have told us | mobile/CLAUDE.md battery contract |
| 45 | 2026-08-04 | advertising capability check = `bluetoothLeAdvertiser != null`, NOT isMultipleAdvertisementSupported | that flag answers "can this controller run SEVERAL advertising sets at once", not "can it advertise". gating on it refuses perfectly capable hardware — we would have shipped a device-exclusion list built on the wrong question. old flag still RECORDED per device as OEM data | BleRadio.android.kt |
| 46 | 2026-08-04 | scan filters on SERVICE UUID, not service DATA | service-data filters are the ones OEM controllers are least likely to offload ⇒ software-filtered scan returns NOTHING with the screen off, which is most of Radar's life. bonus: a Carrier B peer (our UUID, no service data) is still SEEN and recorded as distinct from "nothing there" — which §5.0 needs | BleRadio.android.kt |
| 47 | 2026-08-04 | count Android's 5-scan-starts-per-30s throttle on OUR side (ScanStartGate, budget 4) | the platform's punishment for exceeding it is SILENCE, not an error — indistinguishable from "nobody nearby". an unbudgeted throttle would read as a Phase 0 discovery failure and we would chase the wrong bug | BleRadio.android.kt |
| 48 | 2026-08-04 | PDU connectable flag DERIVED from frame flags bit0 | on-air claim and link-layer reality cannot desync if there is only one source for both | Frame/BleRadio.android.kt |
| 49 | 2026-08-04 | AdvertiseRequest carries an AdvertisePayloadSource (called per epoch), not a ByteArray | the radio then HOLDS no payload, so it cannot cache one across a seam. same structural trick as ephemeralId(ring,day,epoch), one layer down. android-kotlin wrote a parallel AdvertiseAuthority enum then DELETED it — a mapping function between two two-valued fail-closed enums is exactly where SCAN_ONLY becomes ADVERTISE by a typo, in the one control that stops a 2nd device becoming a stalking primitive | 40-contracts |
| 50 | 2026-08-04 | permission audits MUST read the MERGED manifest, never the source one. CI target = build/intermediates/packaged_manifests/**/AndroidManifest.xml | proven the hard way: a source-only review of src/main/AndroidManifest.xml would have declared our permission set clean while the SHIPPED APK asked for RECEIVE_BOOT_COMPLETED (background-persistence signal) and ACCESS_NETWORK_STATE (on an app with NO network permission) — injected by an androidx.work dependency nothing called. transitive manifest merging means the source file is not the artifact | mobile/android manifests |
| 51 | 2026-08-04 | androidx.work DELETED, not annotated. pin kept in the catalog with the reasoning at the point of re-add. | we were paying a store-review cost for a dependency zero code called. deleting beats documenting: an annotation preserves the liability and adds prose. re-adding is now a deliberate act with a manifest change attached | libs.versions.toml |
| 52 | 2026-08-04 | a test that ASSERTS a leak is worse than no test. invert it, do not delete it. | RawSightingTest:35 asserted `rendered.contains("-63")` with the comment "rssi is not sensitive and is useful in logs". BOTH halves false. it was actively holding decision 28's violation in place — converting a leak into something a future fix has to ARGUE WITH rather than simply repair. inverted so the reversal stays visible in the diff, plus 3-figure-negative and positive-dBm coverage so a reformat cannot slip past one hardcoded fixture | RawSightingTest |
| 53 | 2026-08-04 | BOUNDARY_SETTLE_MS must be NUMERICALLY IDENTICAL on iOS and Android. it is not jitter. | fixed and identical on every device ⇒ the whole population still rotates at one instant, satisfying decision 27. a per-platform difference would put iOS and Android devices in SEPARATE ANONYMITY SETS at every boundary — which is decision 27's failure mode reintroduced by an implementation detail rather than by design | BleRadio actuals |
| 54 | 2026-08-04 | superseded account_key is ZEROED at the seam. resolve() faults E_NO_ACTIVE_KEY on destroyed material — same code as "before the ring began". | ADR-008 M4 was a CONDITION OF ACCEPTABILITY, not hardening, and it was unimplemented: KEY_SCHEDULE §8.5.2 was normative text with no code behind it. faulting beats deriving-from-32-zero-bytes, which would produce a valid-looking, WRONG, and IDENTICAL-ACROSS-EVERY-PRUNED-DEVICE identifier | ADR-008 M4 |
| 55 | 2026-08-04 | APPROVED: KeyRing.destroyAllKeyMaterial() for logout + account delete | ADR-008 M5 says hard delete means hard delete, incl backups. a device that keeps its ring after account deletion is the same retroactive-derivation hole M4 closes, just triggered by a different event. new public surface, gated | ADR-008 M5 |
| 56 | 2026-08-04 | a destruction vector must assert the value RESOLVED BEFORE the prune and FAILED AFTER — never only the failure | asserting only the failure passes against a ring that never worked at all. the before/after pair is what makes it a destruction test rather than a broken-ring test. no new hex introduced: every value reused from one already independently proven derivable | key_rotation.json |
| 57 | 2026-08-04 | prune no-ops on a clock rewind and never touches the active or pending entry | destruction is irreversible and the timestamp is the ONE input an attacker might influence. a prune that could be walked backwards into destroying the active key is a remote brick-the-radio primitive | KeySchedule.kt |
| 58 | 2026-08-04 | NO vector count is hardcoded anywhere in devops/ci/. the gate greps the live `VECTORS TOTAL: N` line; VectorManifestTest is the sole authority; the JS pre-check MIRRORS its algorithm rather than keeping an independent one. | the count went 98→99→110→116 in ONE session, and the orchestrator itself handed qa-test a stale number. an independent counting rule had ALREADY silently drifted (missing property_assertions + destruction_at_seam). two counting rules in a repo is one rule and one lie, and you cannot tell which is which without running both | devops/ci |
| 59 | 2026-08-04 | 0xFDA9 release gate STAYS RED. do NOT engineer around it, do NOT allowlist it. | there is no real UUID to guard toward — a flavour guard would just produce a release build with no service UUID, i.e. one that cannot advertise. the gate is not failing, it is REPORTING: we genuinely cannot ship until B9 (legal entity → SIG Adopter → allocation) resolves. a red gate telling the truth beats a green one hiding a blocker. failure message must name B9 so nobody "fixes" it by editing the allowlist | B9, B11 |
| 60 | 2026-08-04 | epoch ticker is DEVICE-scoped (scanning OR advertising, and across adapter-off), NOT advertising-scoped. CORRECTS the orchestrator's own instruction. | I told android-kotlin to drive prune from the advertising restart. WRONG: frameForEpoch only runs on a device that ADVERTISES, and decision 35 makes every other device SCAN_ONLY — yet a scan-only device still holds a full ring (for isOwnEphemeralId, and against the day the role transfers). pruning from the advertising path fixes the HIGH on ONE device per account and leaves it live on the whole rest of the fleet, i.e. exactly the population decision 35 creates. key destruction is not radio work; a phone with Bluetooth off has no business retaining keys it was already required to destroy | ADR-008 M4 |
| 61 | 2026-08-04 | prune runs FIRST, UNCONDITIONALLY, and OUTSIDE the radio lock | first+unconditional because an irreversible security obligation must not sit behind an advertising restart that may be skipped (scan-only), fail (adapter off), or return early. outside the lock because calling foreign code while holding the radio monitor is how a radio deadlocks. a throwing listener does not stop the restart | BleRadio |
| 62 | 2026-08-04 | single-entry rings are NEVER pruned | that is the spike's case and every never-rotated device. destroying that key would make them silently undiscoverable — a privacy fix turning into an availability bug, discovered only by users wondering why nobody sees them | EpochBoundaryPruneTest |
| 63 | 2026-08-04 | selfEidDrops is KNOWN-INCOMPLETE for one epoch per seam, recorded in-source | so a zero is never read as proof nothing was reflected. same discipline as the bijection screen: absence of evidence is not evidence of absence, and instruments must declare their own blind spots | SpikeController |
| 64 | 2026-08-04 | Phase 0 GO/NO-GO thresholds PRE-COMMITTED in docs/PHASE0_GO_NO_GO.md before any measurement exists. NO THRESHOLD MAY BE EDITED once measurement begins. | the predictable failure of a feasibility spike is not bad measurement — it is ambiguous measurement meeting a team that has already spent months and a founder who has already told people what they are building. at that moment "62% co-rotate" becomes a negotiation, not a result: the failing devices get called old, or niche, or firmware-fixable, and the threshold quietly becomes an opening bid. pre-registration removes that conversation — the only question left is which pre-written row the data lands in. a wrong threshold is a FINDING to record, never a bar to move | — |
| 65 | 2026-08-04 | GitHub Actions INTERIM, Gitea DESTINATION. both wired, zero logic in either — every step calls devops/ci/runner/run-stage.sh. | gates existed but ran nowhere = DECORATIVE, which is worse than no gates because it invites everyone to believe checks are running. ADR-003 objects to lock-in and to a vendor holding USER DATA; a build runner executing our own bash against our own source does neither. HARD LINE, non-negotiable: the day CI needs a signing key, registry credential, or prod access it moves to Gitea FIRST — that is when a CI provider starts holding something that matters | ADR-009 |
| 66 | 2026-08-04 | known-red gates use XFAIL (assert the expected state), not "non-blocking" | non-blocking-but-permanently-red trains people to ignore CI, and an ignored gate is a deleted gate with extra steps. so: gate fails ⇒ expected, reason written to the build summary EVERY run, stage passes. gate PASSES ⇒ the world changed, stage FAILS telling you to delete the tolerance line. CI goes red on GOOD NEWS, once. a tolerance that outlives its cause silently absorbs the next real reintroduction of 0xFDA9 — the exact disaster the gate exists to prevent | ADR-009 |
| 67 | 2026-08-04 | CI has THREE layers with one writer each: checks (qa-test) · stage table + blocking split (devops-tencent) · workflow files (discovery only, zero commands) | devops-tencent's correction to my proposal, and it was right: I said put shared logic in the gate scripts, but the blocking split and wrapper selection are RUNNER concerns — that would have devops editing qa-test's files every time a runner concern changed. the layering also makes GitHub/Gitea drift structurally impossible: the two provider files differ in exactly 3 places | ADR-009 |
| 68 | 2026-08-05 | B8 measured PHONE-FIRST. sniffer dongles DEFERRED, not cancelled. founder call. | two phones watching each other already works — the harness bijection screen does it, free, today. buying £30 of dongles up front only pays if you expect a CLEAN result, and expecting a result is what measuring is for. SEQUENCE: phones first ⇒ if BROKEN, answer obtained, no dongles needed ⇒ if CLEAN, that is the untrustworthy case and dongles decide it. | decision 37 |
| 69 | 2026-08-05 | a CLEAN phone-only B8 result MUST NOT be recorded as a pass. | Android never exposes the TxAdd bit, so a permanently FIXED address is indistinguishable from a properly rotating one on-device. the catastrophic case passes the phone test. non-zero = real evidence of failure; zero = NOT evidence of success. anyone reporting phone-only "clean" as a B8 pass has misread the instrument | android-kotlin's harness caveat |
| 31 | 2026-08-04 | shared API v0 gated. entry = create(config,radio,scope) · setVisibility(enum) not setGhostMode(Bool) · ByteArray not hex String · shutdown() AND weak both required | ByteArray reasoning generalises decision 28: a String eid is log-shaped and survives into crash reports + analytics. make privacy-critical values awkward to print | 40-contracts |
